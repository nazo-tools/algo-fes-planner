{
  "name": "algo-fes-planner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "algoフェス2026 はしごプランナー",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node build.mjs"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
